# Removes v from tag
# Include this step before you require the content and write : $(version.cleanValue)
steps:
  - script: echo "##vso[task.setvariable variable=cleanValue;isOutput=true]`echo '$(Build.SourceBranchName)' | cut -d'v' -f2`"
    name: version